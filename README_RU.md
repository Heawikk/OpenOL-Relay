# OpenOL Relay

<p align="center">
  <a href="./README.md">🇺🇸 English</a> /
  <a href="./README_RU.md">🇷🇺 Русский</a>
</p>

<img src="https://github.com/Heawikk/OpenOL-Multiplayer-SC/blob/main/OpenOL_2.gif" width="100%">

TUI/GUI - Relay между клиентами [OpenOL](https://github.com/ShyKiss/OpenOL) — модифицированного клиента игры Outlast.

Relay — это UDP-сервер, который принимает пакеты от клиентов OpenOL и рассылает их остальным игрокам в той же комнате. Сервер не симулирует игровой мир, а пересылает пакеты «как есть» и хранит снапшоты состояния, чтобы подключившийся позже игрок увидел актуальную локацию.

## Возможности

- **UDP-сервер** на 128 клиентов и до 64 комнат
- **Комнаты** с паролем или без, «доверенные» IP (не нужно вводить пароль повторно)
- **Баны** — глобальные и по конкретной комнате, с указанием причины
- **Синхронизация мира** для подключившихся позже: состояния игроков, дверей, врагов и подбираемых предметов
- **Приоритеты игрока с дверью** — пока один игрок взаимодействует с дверью, остальные получают отказ (`DOOR_DENY`)
- **Хранение настроек** в `relay.db` (JSON) рядом с исполняемым файлом
- **Два вида интерфейса**: TUI (консоль с прокручиваемым логом и строкой ввода) и GUI на ImGui
- **Ограничение частоты пакетов** и автоматический таймаут неактивных клиентов

## Конфигурация

Все настройки хранятся в файле `relay.db` (формат JSON), который создаётся рядом с исполняемым файлом. Там же лежат комнаты, пароли, списки доверенных IP и баны:

```json
{
  "config": {
    "name": "OLServer",
    "ip": "0.0.0.0",
    "port": "7777"
  },
  "rooms": [
    {
      "code": "PUBLIC",
      "password": "",
      "trusted": [],
      "bans": []
    }
  ],
  "bans": []
}
```

Файл перезаписывается при изменении настроек через команды/GUI и при завершении работы сервера.

## Команды

Доступны и в TUI, и в поле ввода на вкладке **Log** в GUI.

| Команда | Описание |
| --- | --- |
| `list` | Список игроков по комнатам |
| `rooms` | Список активных комнат |
| `kick <id>` | Кикнуть игрока |
| `ban <id> [reason]` | Глобальный бан по IP игрока |
| `ban room <code> <id> [reason]` | Бан игрока в конкретной комнате |
| `unban <ip>` | Снять глобальный бан |
| `unban room <code> <ip>` | Снять бан в комнате |
| `bans` | Список всех банов |
| `trust add <room> <ip>` | Вручную добавить IP в доверенные для комнаты |
| `trust remove <room> <ip>` | Убрать IP из доверенных |
| `trust list [room]` | Список доверенных IP |
| `room create <code> [password]` | Создать комнату |
| `setpw <code> <password>` | Сменить пароль комнаты (список доверенных IP при этом очищается) |
| `quit` | Остановить сервер |

## Используемое

| Проект | Назначение |
| --- | --- |
| [OpenOL](https://github.com/ShyKiss/OpenOL) | Модифицированный клиент Outlast, для которого создан этот Relay |
| [Dear ImGui](https://github.com/ocornut/imgui) | Графический интерфейс (GUI-сборка) |
| [SDL2](https://github.com/libsdl-org/SDL) | Окно и ввод для GUI-сборки на Linux/macOS |
